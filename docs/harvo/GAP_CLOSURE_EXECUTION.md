# HARVO — Gap closure execution

13 September 2026. Founder instruction HARVO-017 resumes continuous implementation of the documented remaining gaps. The founder now reports hosting/configuration completed, following an earlier hypothetical rating premise. Record the report faithfully; deployed-artifact and pilot acceptance still require observation. No renewed routine implementation permission is needed.

## Impact analysis and implementation plan

Scope: infrastructure, security, financial closure, provider conversion delivery, AI/media integration, targeting usability and operational acceptance. Read Constitution section 20, HARVO, the milestone plan, current runbook, schema and current service contracts before editing each boundary. Preserve additive v2 contracts and financial/provider identities. Existing guest M5/M6B legal/checkout authority is not invented by this work.

| Boundary | Proven gap / proposed correction | Files and impact | Verification and rollback |
|---|---|---|---|
| Deployment | Wrong legacy worker image, public/server output overlap, EOL container runtime, incomplete secret-file exclusions and mixed background ownership | Docker/build/Vercel/Render configuration, startup/static paths, worker/health helpers. No guest booking semantics change. Correct public asset path without exposing server output; keep each mandatory task explicitly owned. | Build and image/runtime regression checks, static source exposure tests, graceful termination and durable progress. Roll back to last verified artifact; do not restart retired paid workers. |
| Financial closure | Final billing verifier has no connected evidence source | Additive settlement evidence/proposal service and migration 012; independent finance-admin review, account/campaign binding, original provider documents and immutable ledger closure. Root wires admin API/UI. | Real PG concurrency/RLS/replay and insufficient/ambiguous evidence tests. Existing funds remain reserved until trusted closure. No inferred final spend from paused status. |
| Conversion delivery | Outbox lacks actual consumer and accepted trusted checkout/consent source | Measurement delivery modules, migration 013, bounded actual Google/Meta transports and runtime tick. Provider acknowledgement is distinct from acceptance. | Controlled HTTP contracts, durable replay/unknown outcomes, per-item acknowledgement and corrections. Missing canonical authority remains explicit; no automatic legacy checkout attachment. |
| Host targeting | Host must type provider resource IDs/country codes; readable location labels can diverge from actual geo resources | Read-only authenticated provider lookup service, bounded shared limits (migration 014 if needed), router/runtime, host selectors and admin delivery labels. No property fields added. | Google response/status/ID checks, request validation, limits, stale search UI/cancellation, draft revision consistency, mobile/keyboard tests. Preserve raw existing IDs for compatibility and require verified resolution before new submission. |
| Operations and discoverability | Workspace hides older campaigns/listings; exception state requires manual record searching | Cursor-based bounded pagination and admin operations view over existing truthful queue data. No broad tenant reads or blind replay buttons. | Stable cursor ordering, tenant isolation, bounded queries, browser recovery/error checks. Keep old first-page contract compatible. |
| AI/media/optimization | Reviewed media/crop utility and recommendations are not fully connected to publishing/validated outcomes | Reverify source then implement bounded immutable media/evaluation and optimization integration where accepted inputs exist. Document separately before extending scope. | Real asset pixel/rights/revision and no-unsupported-provider-operation tests. Never synthesize bookings, policy approval or optimization lift. |

Provider reads run outside DB transactions; bounded service requests share existing credential handling and reveal only public targeting information. No host OAuth is introduced. Mutations keep current content, money and account gates. New migrations are additive and tested on disposable local PG before any staging proposal. Real-provider/payment requests are excluded from tests.

Testing order: focused unit/contract/real-PG regressions per owner; independent integration review; consolidated marketing suite; project typecheck and production build; current-source browser checks; container and operator acceptance. External acceptance includes actual deployment URL, named property/stay dates, bounded per-channel real spend, configured accounts and approved policy/checkout references. Those inputs have been requested while independent engineering continues.

No 10/10, complete-line-by-line audit or 100% milestone claim follows from code existence. Acceptance records must show what actually ran and remaining external constraints.

## Evidence-led continuation after integration review

The first integrated Node24 build/typecheck and compiled process smoke passed. Review identified more executable work, so verification is a checkpoint rather than the end of this instruction.

- Activation now requires accepted code-level booking/capture and consent ports for the selected channel. Meta must bind the configured canonical Purchase pixel to both runtime and observed provider ad set before spending; a safe pause remains available when the binding drifts. Impact: runtime/workflow/provider guards and exact no-write regression tests. No schema or financial rule changes.
- Local inventory/listing protection must not wait behind an external analytics response. Observation scheduling must fairly visit campaigns beyond a persistently failing oldest batch. Impact: engine ordering/scheduling and real-PG outage/fairness tests, without inventing telemetry or retrying uncertain writes.
- Vite's broad `react` substring rule incorrectly captures icon/map/media packages and creates circular manual chunks. Replace it with exact package boundaries; retain service-worker mutation exclusions and source-exposure controls. Verify the actual built chunk graph and browser imports, without raising the warning threshold.
- Complete the existing creative preparation workflow through immutable persistence, explicit host rights and independent exact-byte media moderation, before a reviewed derivative can be selected for publishing. Migration015 is reserved. Root integration must preserve existing campaign revision/media authority. Recheck CDN bytes before use; missing storage remains unavailable. No generated claim or unreviewed media becomes a property fact.
- Add a Google monthly-invoice read/import path only for explicitly configured eligible billing setups, retaining actual response provenance and independent documentary review. Invoice access does not establish finality, campaign allocation or automatic ledger acceptance. Later debit/credit/tax posting still needs a defined accepted accounting policy.

Each boundary remains reversible at the application layer. Additive immutable evidence stays retained during rollback. Real account eligibility, actual CDN/model results, canonical guest acceptance, approved accounting procedure and paid pilot scope cannot be supplied by these code changes.
