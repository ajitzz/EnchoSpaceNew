# Maha execution ledger

2026-09-11 capture-resolution foundation: extracted shared transactional confirmStayCapture and strict locked quote/payment contract. Captured entity ID, amount/currency, zero refunded amount, quote arithmetic and original/locked identity/date binding now gate confirmation. Existing owner signature boundary, current stock/calendar checks, idempotent confirmed replay and audit remain intact. Ordinary tests 303 pass; actual PG 18 checkout + 23 inventory pass; no recovery-state approval, Admin resolution, webhook or refund feature enabled. Sites runtime compatibility prevents deploying the current backend unchanged; no Site or deployment created. Continue durable Admin action identity/actor audit and resolution policy; RESUME.md has exact final verification.

2026-09-11 execution advanced: MAHA-03D-DETAIL Admin read-only saved observation/audit inspection implemented with scope-safe pagination, privacy allowlist and auth/abort UI guards. Before RESOLVE, fixed source-proven stale quote-stock/current-calendar confirmation gap under existing listing lock while retaining contracted price. Early MAHA-06A adds repeatable private PostgreSQL acceptance with sanitized environment, machine-readable receipts and shutdown. Ordinary suite 288 passed / 37 PG skipped; runner separately passed all 37 (14 checkout + 23 inventory). No milestone production promotion, financial resolution or feature enablement. First remaining task is shared audited/idempotent capture resolution, followed by signed webhook/browser recovery and refunds. Final checks and paths in RESUME.md; full staging/browser/provider gates remain open.

2026-09-11 production acceptance contract adopted: MAHA_BLUEPRINT.md now requires MAHA_PRODUCTION_CERTIFICATION.md for each milestone. Added evidence-bound promotion through staging/release approval/observed production, early isolated acceptance infrastructure, per-milestone gates, rollout hard stops and recovery-preserving rollback. Documentation only; prior source changes and verification retained. No milestone promoted, runtime flags enabled or deployment performed. First implementation remains MAHA-03D-DETAIL, then audited resolution/webhook recovery/refunds. See RESUME.md for the active checkpoint.

Continued into MAHA-03D durable payment observations: optional independently gated Razorpay order-payment GET, bounded exact-binding/PII-stripped classification and immutable idempotent observation plus audit. No charge/capture/refund/hold/booking transition. Ordinary suite 267 passed; ten actual PostgreSQL checkout tests passed on fresh RkL0M2 fixture, stopped and retained, in addition to earlier 23 inventory DB tests. Next: Admin persisted-observation details, audited reconciliation/resolution and signed provider recovery, not enabling payments. Final build evidence and exact checkpoint in RESUME.md. All external gates remain open, no production deployment.

2026-09-11 continued autonomously through Admin authorization history, isolated Channex staging identity reader and transaction-safe attestation ingestion, then independent MAHA-03D read-only checkout recovery queue. Provider account/connectivity validation remains open without configured credentials; no live requests made. Twenty-three PG tests passed for inventory/history/ingestion on 37TAqO, stopped and retained. Combined ordinary suite 259 passed / 30 PG skipped. Next: recovery queue SQL/role acceptance and provider reconciliation/observations/audited resolution, while real external provider setup remains a separate gate. Flags off, no production deployment. See MAHA_03C_EXTERNAL_INVENTORY.md and MAHA_03D_CHECKOUT_RECOVERY.md; RESUME.md records final build evidence.

03C writer DB verification (2026-09-11): 20 PostgreSQL tests passed in fresh private Wv8mNc cluster with all four inventory migrations. Concurrent retries/cross-listing claims/revocations and retry SQL constraints verified; prior inventory/registry tests retained. Only tests/docs changed; TypeScript/scoped lint passed, ordinary focused suite eight passed / 20 PG skipped. Cluster stopped and retained. Next: authenticated Admin authorization status/read workflow, then genuine provider ingestion. No production migration, provider action or flags enabled.

03C business authorization writer (2026-09-11): Admin-only default-off grant/revoke routes implemented with owner/listing lock, cross-listing resource lock, bounded expiry, UUID retry/hash protection and retained immutable attribution. No provider attestation can be written through these routes. Staged additive retry metadata migration unapplied. Eight new route regressions; full isolated suite 242 passed / 22 PG skipped, scoped lint passed; build/type evidence in RESUME.md. Next: PostgreSQL writer/idempotency/concurrent revocation tests, then read/status UI and genuine provider attestation ingestion. All flags off; no live grants or provider/deployment operations.

03C registry database verification (2026-09-11): 15 PostgreSQL tests passed in fresh gfM9CF private cluster across all three inventory migrations. Expiry/future facts, revocation, owner transfer, exact binding, server-only RLS and immutable registry checks verified alongside existing lifecycle tests. Test fixture corrected for UUID/text casts and RLS-versus-trigger assertions; no application or migration edits. All test clusters stopped/retained. Next: trusted authorization grant/revocation writers and provider attestation ingestion, resolving integration direction/access before provider calls. No production data changes or feature enablement.

03C connection registry boundary (2026-09-11): added exact listing/current-owner/provider tuple lookup, separate immutable authorization/revocation/provider attestation schema and mapping verifier wiring. No credentials/provider requests, provisioning writer or populated registry. Missing/expired/revoked/ambiguous evidence fails closed; actual SQL semantic acceptance remains next. Channex PMS and booking-channel workflows differ; verify direction/access before implementing provider requests. Next: isolated registry migration/time/revocation/ownership tests, then trusted registry/attestation ingestion. Build/test evidence in RESUME.md. No flags enabled or production migration.

03C shared read-only status UI (2026-09-11): InventoryMappingStatus connected to HostPropertyCards and Admin Properties. Lazy authenticated room selection, abort/late-response guards and explicit disabled/unknown/unmapped/mapped-not-synchronized states; no writes or checkout permission. Six UI regressions pass; full isolated suite 228 passed / 16 PG skipped; scoped lint passed. Build/type-check evidence in RESUME.md. No backend/schema/flag changes. Next: actual server connection registry/attestation and observation ingestion; browser visual/accessibility checks still outstanding.

03C lifecycle implemented locally (2026-09-11): default-off authenticated mapping routes, Admin-only create/revoke, listing lock, canonical room/source checks, server verifier dependency (not installed), current-pointer uniqueness, immutable audit, version conflicts and retry replay. Additional staged lifecycle migration passed with evidence migration in nine real PostgreSQL tests; temporary cluster VCm85e stopped and retained. Full isolated suite 222 passed / 16 PG skipped, production frontend/server build and scoped lint passed. Next bounded task: read-only Host/Admin mapping status with authorization/UI tests; then real connection registry, provider attestation and observation ingestion. Do not enable creation without a real trusted verifier or external checkout without reservation propagation. No production migration/deployment/provider action.

03C database verification (2026-09-11): six isolated PostgreSQL 18 migration/RLS tests passed in fresh inventory_fixture on /private/tmp/encho-inventory-test.4sylUu. Includes listing RLS, unprivileged application role, denied owner inserts, append-only/event/reference checks. Both temporary clusters stopped and retained; no production migration. Assertion corrected to PostgreSQL RESTRICT SQLSTATE 23001 after first run. No application implementation changed in this slice. Next: authenticated mapping lifecycle/current revision/revocation with immutable audit, followed by read-only Host/Admin status; actual provider and reservation propagation gates remain. Details in MAHA_03C_EXTERNAL_INVENTORY.md.

03C external evidence boundary (2026-09-11): staged immutable provider mapping/observation SQL plus server-only full-snapshot adapter validation. Exact mapping identity, bounded date coverage, duplicate/invalid stock rejection and provider/server freshness checks fail closed. No adapter registered, DB writer/API/current-mapping selection implemented, migration applied or external checkout enabled. Focused contract tests pass 23/23; final build/full-suite evidence is recorded in RESUME.md. Next bounded task: isolated migration/RLS verification and an authenticated, audited mapping/revocation lifecycle that selects the current revision under the listing lock, then read-only Host/Admin status. Do not skip provider access and reservation propagation gates. Plan: MAHA_03C_EXTERNAL_INVENTORY.md. MAHA-03 remains incomplete.

03C allocation declaration implemented: inventory_source preserved across Host form/draft/preview, validated by review/Admin, shown in review and guest room cards, enforced by authoritative stay quote. Legacy/unknown/external inventory cannot reach current instant checkout; no host-provided claim of provider verification is accepted. Existing inventory_count means configured Encho units only after explicit allocation declaration. Full suite 187 passed / 7 PG skipped (69788); frontend TypeScript/scoped source code lint passed (95243); final build evidence in RESUME.md. External provider-room mappings, server-owned freshness observations and synchronization remain incomplete; no connector access was purchased or verified. Next bounded task is server-owned external inventory mapping/freshness schema and adapter boundary, not enabling external checkout. Preserve room JSON compatibility and do not backfill allocations automatically.

03C allocation contract plan: additive inventory_source inside canonical room JSON, no SQL schema migration. Supported declarations are unknown, encho_allocation and external_sync. Host must explicitly choose; missing legacy fields mean unknown. Review/Admin validates the enum and displays each room source; guest detail explains booking eligibility; checkout fails closed except reviewed Encho allocation. External-sync declaration is not connector evidence and cannot unlock checkout. Preserve source through draft/upload/preview payloads, preserve zero inventory, update quote fixtures to explicitly represent allocations. No actual integrations or fake freshness/mapping IDs. Test missing/external source rejection, reviewed property validation and guest labels. Current checkout remains disabled globally.

03B dated projection implementation: projectCampaignAvailability uses shared dated inventory with listing-scoped bookings/holds and calendar blocking after authorization. Missing checkout schema/invalid history/removed rooms report unknown. Host/Admin show aggregate room availability and freshness for saved scope, never guest rows or external/delivery clearance. Full isolated run passed 179 tests / 7 PG skipped (66998); an additional UI-label test was subsequently added, with final check evidence in RESUME.md. No new schema/flag changes. Next bounded task: MAHA-03C inventory source/mapping contract (explicit Encho allocation versus synchronized external inventory, freshness and missing mapping behavior) across Host property setup, Admin review and guest checkout; inspect before implementation. 03B database/role/browser/performance and provider activation acceptance remain open.

03B availability projection plan: after campaign/property authorization under existing locks, use a transaction-local internal RLS bypass only for listing-scoped aggregate inventory reads. Never return reservation records/PII. Require the staged checkout table before claiming internal availability; absent schema yields unknown. Reuse stayAvailability for selected room IDs and nights, respect calendar booked/blocked, retain expired-ready filtering and indefinite creating/review holds. Shared Host/Admin display labels this internal-only with timestamp and unsaved-edit warning. No provider control or schema mutation. Tests inject real projection queries for holds, blocks, malformed/missing inventory and missing schema; rollback preserves existing writes if reads fail.

Campaign edit boundary implemented: campaign lock precedes ownership/state validation; content, transition and sanitized audit are atomic. Generic edit is restricted to unapproved drafts without known provider objects and cannot request active state. Campaign video no longer bypasses property review. Full isolated suite 172 passed / 7 PG skipped (4874); final token-redaction change passed all 7 extracted-route tests (88346). Frontend TypeScript/scoped test lint passed (43958); server compilation initially passed (30736), final server compilation recorded in RESUME.md. No frontend change this slice; previous frontend build remains historical evidence. Next: scoped read-only dated availability projection, then real DB/UI integration and remaining provider identity/concurrency gates. Feature flags remain off.

Campaign PUT impact: read/approval invalidation occurs before transaction, general status input can request lifecycle changes, and video edits bypass property review. Keep existing payload schema and response; acquire campaign FOR UPDATE before reading/validating, restrict generic edits to unapproved drafts with no provider objects and draft/submission targets, invalidate policy artifacts for draft content changes, audit full state within the write transaction. Do not modify listings from marketing. Preserve dedicated provider controls and transition service. Tests execute extracted real route with injected SQL/FSM, covering lock order, status/provider gates, rollback and no listing writes. No schema change or provider calls. Rollback disables generic edits rather than restoring races.

03B review acknowledgement: implemented explicit Admin displayed-version acknowledgement, locked server comparison before approval/idempotency, and provider-object draft-edit guard. Added route/UI regression tests. Fresh suite 165 passed / 7 opt-in PG skipped (16567). Correction: previous build 75271 failed at server TypeScript after frontend bundling; implicit-any callback is now fixed. Fresh compilation results must be taken from RESUME.md, not the earlier premature build claim. Next task remains legacy campaign PUT transaction/approval race and direct listing-video publication bypass, then dated inventory projection. Feature stays disabled; no migration or provider operations.

MAHA-03B partial implementation checkpoint: staged nullable stay_scope migration; gated authenticated owner/admin GET + draft-only PUT; exact room/date validation; scope-version conflict checks; atomic audit; policy/approval artifact invalidation. Approval hash includes non-null scope. Shared CampaignStayScope Host editor/Admin reader is connected to campaign cards. Flag remains off; migration not applied. No external availability claim or provider call. Ten new API/validation tests; full suite 159 passed / 7 PG skipped (24897), build passed (75271), frontend TypeScript passed (74583), scoped lint/diff checks passed (1695). No processes pending.

First remaining 03B task: close exact displayed-scope approval acknowledgement and audit legacy campaign mutation races/provider-object edits before enabling the flag; add shared UI regressions and the scoped read-only dated availability projection. Current route intentionally does not enable date-aware delivery; actual DB migration/permissions/lock-order verification remains a release gate. Existing campaign PUT can update listing video outside property review and reads campaign state before its transaction; these source-proven connected gaps must be resolved, not assumed covered by the new scope handler.

MAHA-03B plan: additive stay_scope JSONB column on host_marketing_campaigns, migration not applied by boot. Separate authenticated draft-only scope API with campaign/listing locks, ownership validation, exact room IDs/dates, optimistic version and transactional audit. No edits to delivering/submitted campaigns; existing scope is included in approval hash. Shared Host/Admin component displays exact scope and distinguishes unknown external availability. Feature flag defaults off until migration verified. No provider calls or automatic resume. Tests execute injected handlers/contracts; rollback disables editor and preserves persisted scope/audit. Existing campaign creation/update routes retain the field rather than interpreting ad duration as stay dates.

Detailed product/architecture and bounded milestone blueprint: [MAHA_BLUEPRINT.md](MAHA_BLUEPRINT.md). This expands the existing execution, not a restart.

MAHA-03A locally verified: shared lib/stayAvailability.ts is used by stayCheckout.ts. It evaluates exact room IDs and minimum remaining units across half-open nights, preserves zero stock, treats unidentified legacy rooms conservatively and rejects malformed/reversed history rather than truncating/ignoring it. Existing quote calendar blocking and SQL hold/status selection remain intact. No new fields/migrations or advertising activation. 149 isolated tests passed / 7 database tests skipped (39540); frontend/server build passed (99265); frontend TypeScript, scoped new availability lint and diff check passed (10862). Next bounded task is MAHA-03B persisted, reviewed campaign stay-date/room scope and its connected availability projection; external availability remains unknown until verified. No pending processes.

MAHA-03A impact/plan: extract a shared pure dated room-availability contract from stay checkout. Existing inline logic handles half-open nights but silently ignores reversed reservation ranges, truncates malformed dates and conflates unknown inventory with availability. Validate exact room identity, bounded requested dates, integer inventory and every applicable reservation interval. Evaluate minimum remaining units across nights, accounting for owner-scoped exclusions of an existing hold by the caller. Preserve existing SQL/state/transaction/RLS and checkout error contracts. No campaign fields or migration introduced in this slice; no external availability attestation invented. Tests cover consecutive reservations, overlaps, checkout boundary, unknown-room reservations, zero stock and invalid history. Campaign target-date persistence, external feeds, quote integration and admin review remain follow-up work. Rollback by disabling checkout, not restoring invalid-date acceptance.

Room consistency checkpoint: canonicalInventory and assertRoomIdentity now connect review/Admin/circuit-breaker contracts. Existing IDs cannot disappear in a published edit; retain the room with zero inventory. Admin no longer destroys normalized room rows. Review and Admin post-commit hooks invoke the existing circuit breaker. Host known published-room removal sets zero and explains review. Breaker only infers unavailability from canonical zero; missing/positive inventory remains date-unverified and cannot auto-resume ads. Legacy normalized records remain fallback, not current inventory for canonical listings. No migration or actual provider calls.

Evidence: 136 isolated tests passed / 7 database tests skipped (64708); frontend/server build passed (48351); frontend TypeScript passed (37118); scoped new/changed room code lint passed (75151), final breaker/review-test lint checked separately; diff whitespace passed. Tests include zero/malformed/duplicate IDs, canonical/legacy identity retention, Admin and approval rejection of lost IDs, absence of destructive room projection, zero-stock pause and no positive/unknown-stock resume. Full dated occupancy remains MAHA-03/05. Next implementation is the dated availability contract and checkout recovery in MAHA-03; MAHA-02 browser/DB acceptance and archive workflow remain open, not counted as fully complete.

Room consistency plan: listings.rooms is canonical for reviewed publication, Admin edits and checkout. Stop destructive normalized room replacement; retain room_types only as historical fallback for unmigrated listings. Existing room IDs must remain present on edits (set inventory to zero to retire), including legacy fallback IDs. Shared locked validation rejects identity loss. Circuit breaker must read canonical rooms, preserve zero, and never infer date availability from lifetime booking counts; positive/unknown inventory skips automated controls until the dated campaign contract is implemented. Zero canonical inventory can pause but cannot resume. No schema change. Test identity retention, zero/unknown inventory and no automatic resume; preserve audit and transactions. Rollback by disabling edits/automatic controls, not restoring destructive projections.

Deletion checkpoint: implemented and isolated regressions pass. New propertyDeletion.ts replaces legacy DELETE; Admin authentication/error feedback and Host confirmation copy are connected. 122 tests passed / 7 database tests skipped (95929, also frontend TypeScript exit 0); production frontend/server build passed (29767); scoped deletion lint and diff checks passed (85684). Tests cover malformed IDs, ownership under lock, history/checkout rejection, snapshots, post-commit notification and rollback. No real records deleted. Archiving used properties and production FK/RLS/cache verification remain open.

Next room compatibility evidence: src/lib/calendarCircuitBreaker.ts reads inventory SUM from room_types, whereas stay checkout and property views use listings.rooms. Admin edits currently delete/recreate room_types; media_assets.room_type_id is SET NULL on deletion. The review publisher does not project room_types. Thus merely removing legacy projection writes would leave the circuit breaker stale. Resolve this shared inventory contract with stable identity and connected tests before marking MAHA-02 complete; no speculative projection change was made in the deletion task.

MAHA-02C deletion plan: schema inspection proves cascading loss of bookings, threads/messages, reviews, campaigns/leads and social posts. Preserve every operational-history record: reject deletion when any reference exists, including drafts, wishlists, calendar entries and optional staged checkout orders. For unused properties only, lock the listing before owner/admin authorization, snapshot listing/rooms/media in the immutable audit, delete media and listing atomically. FK insertion locks serialize references against the listing lock; no schema change or runtime DB inspection outside the injected handler. Admin caller must send authentication and display rejection details; Host already retains cards on errors. Test malformed IDs, owner isolation, dependencies, rollback and callback ordering. Rollback is disabling deletion, never reinstating cascading history loss. Archiving used properties remains a separate connected workflow, not a silent semantic change to DELETE.

Pagination checkpoint: locally verified. Fresh suite 112 passed / 7 opt-in PostgreSQL skipped (17642); production frontend/server build passed (1400), frontend TypeScript check passed (63401), scoped pagination API/UI/test lint passed (11695). Eight new tests cover scoped cursors, overflow/malformed values, lookahead, page failure/retry, deduplication and refresh. No pending commands or external writes. Historical latest-100 limitations below are superseded by this checkpoint.

Next source-backed task: legacy DELETE /api/listings/:id in server.ts returns fabricated success for nonnumeric IDs, checks ownership outside its deletion transaction and hard-deletes without audit. Review all dependent foreign keys and booking/campaign references before selecting a history-preserving removal contract. room_types has a cascading listing foreign key; media_assets.room_type_id is SET NULL on room deletion, so replacing normalized rows can discard media associations. These are open findings, not fixed by pagination.

MAHA-02C pagination impact: the queue silently bounds history to 100 records. Add optional validated `before` ID cursor and additive `nextCursor` response, retaining `submissions`/`limit`. Order by immutable descending submission ID rather than mutable updated_at to avoid edit-induced page drift. Fetch 101 rows for a 100-row page; preserve owner/admin predicates on every request. Shared UI appends deduplicated pages, retains rows/cursor on errors and aborts stale requests. No schema or guest contract change. Tests cover cursor validation, tenant predicates, lookahead, append/retry and refresh. Rollback only pagination UI/API additions; preserve review decisions and existing changes. This is live pagination, not a frozen snapshot of a changing moderation queue.

Owner authorized execution on 2026-09-10. This plan extends the existing connected transformation; verified work is retained. Quality is established by acceptance evidence, not a subjective 10/10 claim.

| ID | Scope | Status | Acceptance |
| --- | --- | --- | --- |
| MAHA-01 | Calendar authorization, validation and atomic mutation | verified locally | 33 targeted calendar/stay tests, type check and production build pass |
| MAHA-02 | Host/Admin workspace and listing workflow | in_progress | Responsive accessible workflows; draft/review/version consistency across guest, host and admin |
| MAHA-03 | Inventory, checkout recovery and cancellation | in_progress | 03A internal dated evaluator locally verified; campaign scope, external mappings, recovery and refunds remain |
| MAHA-04 | Campaign builder, AI review and admin controls | todo | Reviewed version binding, failure fallback, funding separation, audited delegated permissions |
| MAHA-05 | Real Meta/Google operations, funding and CRM | todo | Provider hierarchy/control/reconciliation, ledger, delivery adapters and identity linking verified |
| MAHA-06 | Isolated staging and release acceptance | todo | Role E2E, accessibility, performance, security, provider canaries and rollback evidence |

Dependencies: MAHA-01 precedes inventory rollout. MAHA-02 preserves completed workspace components. MAHA-03 and MAHA-04 contracts precede MAHA-05 activation. MAHA-06 requires all preceding acceptance criteria. Break each remaining scope into independently reviewable subtasks before its implementation; never count a partial scope complete.

## MAHA-01 impact and plan

Source-proven defect: POST /api/listings/:id/calendar authenticates but does not check property ownership, validates neither dates nor monetary/status fields, and commits each date separately. HostCalendar hides non-success responses.

Category: Security, connected UI. Keep the route and existing calendar schema. Validate bounded real dates, nonnegative finite prices, offer references and supported states. Lock the listing and enforce owner/admin authorization before mutations. Save the complete batch and audit event in one transaction; invoke the existing ad circuit breaker only after commit. Surface server failures in the host form. No new property fields or migration. Guest reads and admin callers retain the API contract; invalid/demo IDs now fail explicitly.

Risk: legacy malformed clients will be rejected; audit insertion failure must roll back the calendar. Existing circuit-breaker date semantics and external synchronization remain MAHA-03/05 work. Tests execute the real handler with injected SQL dependencies, without booting server.ts or connecting to Neon. Rollback: disable the affected edit control if necessary; preserve history and never restore unauthorized writes.

Progress accounting: report verified task IDs and remaining gates. No inherited percentage ratings. Save current work and exact next action in RESUME.md after every coherent task.

## MAHA-01 evidence

Changed server.ts, components/HostCalendar.tsx, src/server/stayCheckout.ts and added src/test/platform-calendar.test.ts. The calendar handler now enforces owner/admin permissions, validates dates/price/status/offer, locks the listing, records a transactional audit and commits all dates together. Host UI surfaces rejected saves. Stay quote treats manually booked dates as unavailable alongside blocked dates.

Targeted isolated command: npm exec vitest -- run --config vitest.platform.config.ts src/test/platform-calendar.test.ts src/test/platform-stay.test.ts — 33 passed. Ten calendar tests cover unauthorized owners, admin access, malformed input, audit ordering and rollback. SQL behavior is dependency-injected; actual PostgreSQL transaction behavior was not re-tested in this task. Full tsc --noEmit passed before final quote change. Production frontend/server build passed (session 74007 exit 0); existing NODE_ENV warning remains. git diff --check passed. No live database, gateway or advertising operations executed.

## MAHA-02 bounded tasks and impact

- 02A (verified locally): reviewed listing submission, host submission status, admin review queue and exact-version approval.
- 02B (implemented core flow; full form accessibility/validation still open): versioned draft save/resume, requested-change recovery, truthful form/media/location defaults and published/submitted comparison.
- 02C (in_progress): property cards, transactional admin edits and initial form accessibility implemented; pagination, deletion boundary, projection compatibility and browser acceptance remain.

02A source evidence: HostForm directly publishes and then silently duplicates draft writes; startup auto-promotes unreviewed drafts; the old draft approval route has no admin-role guard. The old draft publisher also omits listing fields. Plan: a dependency-injected review router using existing listings_drafts and admin_audit_logs, explicit allowlisted listing fields, locked status/version checks, owner-bound linked listing IDs and atomic publication/audit. Host form submits for review; shared host/admin review UI displays persisted state and full submitted fields. Disable automatic draft promotion and host direct-publication bypasses; legacy admin direct edit remains a separate audited-workflow gap. No migration or external operations. Existing published records remain served to guests until approval. Risk: new pending submissions require the new review queue; normalized legacy room/media sidecars need later compatibility verification. Test owner isolation, role gates, stale version, repeat approval and audit failure using injected dependencies. Rollback via controlled maintenance of submission routes, never reinstating automatic publication.

## MAHA-02 implementation checkpoint

- New `src/server/propertyReview.ts` handles private draft save, owner-bound resume/version checks, submission idempotency, required room/media validation and admin-only version-bound decisions. Edits compare the published baseline; concurrent changes cannot be silently overwritten. The reviewed listing and media_assets projection commit with audit; saved/submitted draft snapshots are retained in audit events.
- `PropertyReviewQueue` is shared between host Properties and the Admin Property review tab. It displays persisted state, rejection notes, draft/revision recovery, actual photos and published/submitted field comparisons. The queue is currently bounded to the latest 100 records; pagination remains 02C work.
- `HostForm` saves incomplete drafts and submits complete properties for review. Duplicate clicks are guarded; unchanged retries reuse the uploaded payload and request key. Unresolved uploads fail rather than embedding base64 into listing data. Saved photo metadata and room IDs are preserved. Unfounded preset room descriptions, facilities, stock-photo padding and verification/review figures were removed from defaults/preview.
- `LocationPicker` accepts saved coordinates, does not reverse-geocode a default center into the property on mount, preserves entered address text and stores coordinates only on user selection. Null coordinate displays are handled.
- Startup automatic draft publication was removed. Legacy unversioned approval returns 410. Host direct listing POST/PUT must use review; legacy draft writes cannot modify submitted records. Existing admin direct edit/delete paths still need 02C audit and transactional review.
- JSON rooms remain canonical; legacy room_types synchronization/compatibility is an explicit remaining acceptance gate. External ad content/pricing synchronization remains MAHA-04/05. No migrations, live DB operations, provider calls or deployment were performed.

Verification: final isolated suite passed **89 tests**, with 7 opt-in PostgreSQL tests skipped (session 51231, exit 0). This includes 13 property-review API tests and 5 review UI tests. Final production frontend/server build passed (session 71878, exit 0); scoped new-code lint passed (session 72312, exit 0); diff whitespace checks passed. Existing NODE_ENV warning remains. These tests inject database dependencies and do not establish production schema compatibility, RLS enforcement or browser E2E. No checks are pending.

Next bounded work after verification: MAHA-02C published-property cards (currency, missing images, failed-delete handling), admin edit transaction/audit boundary, and full HostForm accessibility/validation. Preserve the completed review/draft flow.

### MAHA-02C impact and implementation plan

Confirmed source defects: host cards prefix all rates with USD, insert stock images when media is absent, and remove a card after any HTTP delete response. Admin PUT /api/listings/:id commits field and projection writes separately, swallows projection failures and lacks an audit transaction. Changes will preserve route envelopes and the existing schema: add an accessible searchable property-card component with explicit loading/error/action states, and make admin editing use a locked transaction with audit and post-commit provider synchronization. Avoid unrelated reservation, experience or campaign redesign. Test card failure/retry/currency behavior and real extracted admin handler role/rollback/audit boundaries. Roll back only scoped components if needed, preserving submitted drafts and audit history. No production DB or provider calls during verification.

### MAHA-02C verification checkpoint

Implemented HostPropertyCards search, source-currency formatting, neutral missing-image state and confirmed deletion with retained cards on HTTP failure. Admin PUT uses adminListingUpdate.ts with locked partial updates, room/media projection rollback and full audit. HostForm adds 23 accessible field names, current-step announcement, validation-based completion marks and disables editor inputs during submission; title guidance now matches the ten-character validation minimum. This is not full WCAG certification.

Fresh isolated suite: 104 passed, 7 opt-in database tests skipped (24353). Production frontend/server build passed (56576), scoped ESLint on new cards/admin handler/tests passed (26639), and git diff --check passed. Existing NODE_ENV build warning remains. No live database, payment, advertising or deployment operations occurred. Next: review-queue pagination beyond the latest 100 records, followed by legacy deletion and room projection compatibility. Existing post-commit pricing sync is best-effort and still requires durable provider reconciliation in MAHA-04/05.
