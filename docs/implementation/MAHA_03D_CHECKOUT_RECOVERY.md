# MAHA-03D checkout recovery

Category: Payment / Security / UI. Status: triage queue implemented; financial reconciliation incomplete.

## Shared capture contract — 2026-09-11

Source finding: /verify validated provider payment against its pre-network order, then used an independently loaded quote after locking without proving those contracts still matched. Payment entity ID and refund amount were not checked at that final boundary. Impact: lib/stayCaptureContract.ts, src/server/stayCaptureConfirmation.ts, stayCheckout.ts and regression fixtures. No schema or property field change; existing guest verification response shape preserved, invalid evidence now returns explicit 409 review instead of possible confirmation. Host/Admin booking consumers remain unchanged because persisted booking shape is unchanged.

Implementation: a pure validated contract binds checkout owner/property/room/dates/provider order/amount/currency to the exact unchanged quote, verifies integer component sums and night span, and requires the fetched payment's exact ID and zero refunded amount. SQL reads DATE::text to avoid timezone reinterpretation. Shared confirmStayCapture locks listing before checkout, enforces that contract, preserves current fulfillment/calendar checks and atomically inserts booking, checkout confirmation and audit. Confirmed replay checks the persisted booking/payment/room identity. Caller retains authorization, transaction-local RLS and commit ownership; provider fetch/signature checks remain outside the database transaction. No admin role bypass, charge, capture, refund or hold release is introduced; creating/review/expired recovery states remain unsupported.

Evidence: 303 ordinary isolated tests passed; 41 PG tests skipped in that run and separately passed as 18 checkout plus 23 inventory. Actual PG cases change quote/amount during provider I/O and inject refund/payment-identity conflicts; all reject without booking or confirmation event. Latest stopped fixtures: 8LisRT/YJ1C23, with acceptance.json receipts. Full final build/lint evidence in RESUME.md. Rollback disables checkout if necessary, never restores acceptance of mismatched financial evidence. This is a shared safe confirmation prerequisite, not completed Admin financial reconciliation. Next: durable Admin action identity/actor audit and explicit resolution policy, then signed webhook recovery and refunds.

## MAHA-03D-DETAIL — implemented and database-verified locally, 2026-09-11

Fulfillment prerequisite subsequently implemented: /verify now reads current canonical rooms/currency under its existing listing lock, validates exact unique room identity, Encho source, availability/capacity/current stock and fresh calendar blocked/booked rows after provider I/O. Shared currentStayInventory returns current stock without repricing the immutable quote. Existing confirmed-order replay remains idempotent; rejected new confirmations do not create a booking or release a hold. No refund or Admin confirmation transition is added. Twelve pure regressions and three actual PG provider-I/O race cases passed. Latest ordinary suite 288 passed; 14 checkout plus 23 inventory PG tests passed through the new isolated runner. See RESUME.md for final build evidence. Next: shared audited capture-resolution boundary with locked payment/quote binding and durable idempotent outcome; signed webhook recovery and refunds remain open.

Source gap: Admin could not inspect persisted observations or checkout audit history. Added Admin-only GET /api/stay-recovery/:checkoutId and lazy expandable StayRecoveryDetails inside the existing queue. Same default-off recovery gate, no provider request or mutation. A repeatable-read/read-only transaction returns an allowlisted order, at most 20 observations and 50 audit events with independent keyset cursors. Cursors must belong to the selected checkout; invalid/missing/foreign references fail explicitly. Historical payment JSON is revalidated/stripped of non-allowlisted fields; responses use no-store. Authentication changes/collapse abort reads and clear history; failed pagination retains loaded evidence. No new property fields or Guest/Host state change, so their existing contracts remain unchanged.

Tests: 22 focused detail/recovery/observation tests passed, full ordinary suite 276 passed / 34 PG skipped; 11 actual checkout PostgreSQL tests passed on fresh D5jiFr fixture including read-only transaction, scoped cursor pagination, role denial and no state mutation. Frontend TypeScript and new detail source/test lint passed. Final frontend/server build status belongs in RESUME.md; browser/staging/provider verification remains open. Database fixture is synthetic only. Rollback disables recovery or removes the detail entry point without deleting observations/audit. No schema or production changes.

Next in MAHA-03D-RESOLVE: inspect and strengthen the shared confirmation prerequisites before adding an Admin resolution transition. Source inspection found /verify takes a listing lock but calculates availability using the old quote.inventory and does not recheck new calendar blocks/current room source. A host/Admin stock reduction after order creation could therefore be ignored. Fix this proven prerequisite with current canonical stock under the same listing lock while preserving the agreed quote price; test changes during provider I/O and late capture. Do not add an unsafe Admin confirm/release control.

## Durable payment observations — 2026-09-11

Added Admin-only POST /api/stay-recovery/:checkoutId/observe with UUID operation retry identity. The server reads only the stored Razorpay order, never a client-supplied payment or replacement order. Optional SDK orders.fetchPayments adapter is separately gated by STAY_RECOVERY_PROVIDER_ENABLED; both recovery flags default off. Official read-only endpoint reference: https://razorpay.com/docs/api/orders/fetch-payments/ . No provider call was executed here.

summarizeOrderPayments requires complete bounded collections, unique IDs, safe integer amounts and exact order/currency/amount binding. Its allowlist drops contact/card data. Results are captured_observed, no_capture_observed, refund_review or provider_review, never payment-failed/safe-to-release. Refund evidence and multiple captures require review. No provider collection error can be substituted with an empty successful response.

observeStayPayment first checks persisted identity/idempotency, releases its transaction before the network read, then locks listing before checkout and revalidates stored amount/currency/order before inserting immutable observation plus checkout audit event atomically. Retry returns the existing operation and does not refresh its freshness. No booking, hold, provider-order, payment or refund state is mutated. Audit logs identify checkout/operation on failures without logging raw provider payload or credentials. The staged stay_payment_observations migration adds server-only RLS and append-only history. No production migration or financial operation occurred.

Eight additional isolated payment-observation tests pass. All ten real PostgreSQL checkout tests passed in a fresh private /private/tmp/encho-stay-test.RkL0M2 cluster, including observation idempotency/concurrency, no checkout-state change, immutable observations, missing-order refusal and Admin recovery query. Fixture date allocation now advances correctly across year boundaries as tests grow. Cluster stopped and retained. SQL fixture does not establish live Razorpay canary, full production-role grants or resolution safety. Final ordinary suite before last PG-only additions: 267 passed / 30 opt-in PG skipped. Current PG suite has 10 stay tests plus 23 inventory tests (33 opt-in total).

Next: show persisted observations in Admin checkout details and implement carefully audited resolution/recovery policy, with captured-payment/late-inventory contention and refund idempotency. Unknown order creation must be reconciled from genuine provider evidence; missing references cannot be guessed. Live provider checks and signed webhook/browser-abandonment recovery are open. This milestone remains incomplete.

Source finding: creating/review holds and expired ready attempts lack an Admin recovery view. Unknown provider creation/capture outcomes cannot be solved by blindly retrying payments or releasing stock. Preserve the staged checkout state machine and immutable events.

Plan/impact: new authenticated Admin-only, default-off GET /api/stay-recovery exposes a paginated read-only queue for creating/review/expired and expired-ready attempts. Query returns exact checkout/listing/room/provider references and amounts, never guest names/phone, quote payloads, retry keys or credentials. No schema or financial writes. Transaction-local server RLS bypass remains bounded to the authorized route. Cursor order is immutable created_at/id; this is a changing queue, not snapshot isolation. No total-count scan. Missing schema/failed reads explicitly return unavailable.

Admin gets a Checkout recovery navigation item with queue, reasons, exact integer-minor-unit display, refresh, retry and older-page loading. Authentication changes clear the previous queue. No charge/refund/confirm/release buttons exist; an expired attempt does not establish failed payment. Host/Guest booking status remains unchanged because no state transition is introduced.

Verification: three route and two UI regressions added; full isolated suite 259 passed / 30 opt-in PG skipped. Final build/type/lint evidence in RESUME.md. Actual recovery SQL query/schema acceptance and browser accessibility/layout remain outstanding. No live provider or DB requests. Rollback disables STAY_RECOVERY_ENABLED or removes the read-only entry point; never delete checkout records or release unresolved holds.

Next: isolated recovery SQL/role acceptance, provider reconciliation with durable observations, ownership-safe details and audited resolution; capture/inventory contention and refund requirements must be handled before enabling checkout. Do not invent provider outcomes to clear the queue. Provider calls must remain outside listing/checkout locks, followed by locked state revalidation before a permitted transition.
