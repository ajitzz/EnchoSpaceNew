/** Versioned product copy; deployment must bind exactly this reviewed version.
 * A new legal disclosure requires new copy/version, never blind acceptance. */
export const assistanceDisclosure={version:'cr1-service-assistance-v1',
  text:'Requesting Encho assistance lets assigned support staff read this conversation and keep internal support notes. Either participant can stop new support access. Stopping access does not delete support records already created.',
} as const;
